name: pagination_advanced
description: Advanced pagination solution for Flutter with refresh, search, filters, and infinite scrolling.
version: 1.0.1
repository: https://github.com/Fayis2021/pagination-advanced

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  equatable: ^2.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
